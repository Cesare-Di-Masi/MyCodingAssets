<Window x:Class="Presentation_WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Blog Manager Workspace"
        Height="760" Width="1150"
        WindowState="Maximized"
        Background="#F0F2F5"
        FontFamily="Segoe UI"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <SolidColorBrush x:Key="BrandBlue" Color="#2563EB" />
        <SolidColorBrush x:Key="BrandBlueHover" Color="#1D4ED8" />
        <SolidColorBrush x:Key="SurfaceWhite" Color="#FFFFFF" />
        <SolidColorBrush x:Key="TextPrimary" Color="#1F2937" />
        <SolidColorBrush x:Key="TextSecondary" Color="#6B7280" />
        <SolidColorBrush x:Key="BorderLight" Color="#E5E7EB" />

        <DropShadowEffect x:Key="CardShadow" BlurRadius="10" ShadowDepth="2" Direction="270" Color="#111111" Opacity="0.05" />

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Background" Value="#F9FAFB" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource BrandBlue}" />
                                <Setter Property="Background" Value="{StaticResource SurfaceWhite}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderLight}" />
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="{StaticResource BrandBlue}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource BrandBlueHover}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="SecondaryButton" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#E5E7EB" />
            <Setter Property="Foreground" Value="#374151" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D1D5DB" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="DangerButton" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#FEE2E2" />
            <Setter Property="Foreground" Value="#DC2626" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FECACA" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder" Background="White" BorderBrush="{StaticResource BorderLight}" BorderThickness="1" CornerRadius="6" Padding="12">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#F3F4F6" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{StaticResource BrandBlue}" />
                                <Setter TargetName="ItemBorder" Property="BorderThickness" Value="2" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="ArticleItemTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="15" Foreground="{StaticResource TextPrimary}" Margin="0,0,12,4" TextTrimming="CharacterEllipsis" />
                    <TextBlock Text="{Binding Content}" FontSize="13" Foreground="{StaticResource TextSecondary}" TextTrimming="CharacterEllipsis" MaxHeight="38" />
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="Created: " FontSize="11" Foreground="#9CA3AF" />
                        <TextBlock Text="{Binding CreatedAt}" FontSize="11" Foreground="#9CA3AF" FontWeight="SemiBold" />
                    </StackPanel>
                </StackPanel>

                <TextBlock Grid.Column="1" Text="{Binding Id, StringFormat='#{0}'}" FontSize="12" Foreground="#D1D5DB" FontWeight="Bold" VerticalAlignment="Top" Margin="8,0,0,0" />
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400" MinWidth="320" MaxWidth="550" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource SurfaceWhite}" CornerRadius="8" Padding="16" Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,16">
                    <TextBlock Text="Articles Archive" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,12" />

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding SearchTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0" ToolTip="Search by title..." />
                        <Button Grid.Column="1" Style="{StaticResource PrimaryButton}" Content="Search Title" Command="{Binding SearchByTitleCommand}" Margin="0" />
                    </Grid>
                </StackPanel>

                <GroupBox Grid.Row="1" Margin="0,0,0,16" Padding="8" BorderBrush="{StaticResource BorderLight}" Foreground="{StaticResource TextSecondary}">
                    <StackPanel>
                        <WrapPanel Orientation="Horizontal" Margin="0,4,0,8" VerticalAlignment="Center">
                            <DatePicker SelectedDate="{Binding SearchDate}" Width="130" />
                            <Button Style="{StaticResource SecondaryButton}" Content="Search Date" Command="{Binding SearchByDateCommand}" />
                            <Button Style="{StaticResource SecondaryButton}" Content="Reset" Command="{Binding ClearFiltersCommand}" />
                        </WrapPanel>
                        <WrapPanel Orientation="Horizontal">
                            <Button Style="{StaticResource SecondaryButton}" Content="Reload List" Command="{Binding LoadArticlesCommand}" />
                            <Button Style="{StaticResource PrimaryButton}" Content="+ New Article" Click="AddArticleBtnClicked" />
                        </WrapPanel>
                    </StackPanel>
                </GroupBox>

                <ListBox x:Name="ArticlesListBox" Grid.Row="2"
                         ItemsSource="{Binding Articles}"
                         SelectedItem="{Binding SelectedArticle, Mode=TwoWay}"
                         SelectionChanged="ArticlesListBox_SelectionChanged"
                         ItemTemplate="{StaticResource ArticleItemTemplate}"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled" />

                <StackPanel Grid.Row="3" Margin="0,12,0,0">
                    <Separator Margin="0,0,0,8" Background="{StaticResource BorderLight}" />
                    <TextBlock Text="{Binding Articles.Count, StringFormat='Total shown: {0}'}" Foreground="{StaticResource BrandBlue}" FontWeight="SemiBold" />
                </StackPanel>
            </Grid>
        </Border>

        <GridSplitter Grid.Column="1"
                      Width="6"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Stretch"
                      Background="Transparent"
                      Cursor="SizeWE"
                      ShowsPreview="False" />

        <Border Grid.Column="2" Background="{StaticResource SurfaceWhite}" CornerRadius="8" Padding="24" Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Article Editor" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,16" />

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="Record ID:" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <Border Background="#F3F4F6" CornerRadius="4" Padding="6,2">
                        <TextBlock Text="{Binding SelectedArticle.Id, TargetNullValue='-'}" Foreground="{StaticResource TextPrimary}" FontWeight="Bold" />
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="Created At:" FontWeight="SemiBold" Foreground="{StaticResource TextSecondary}" Margin="0,0,8,0" VerticalAlignment="Center" />
                    <Border>
                        <TextBlock Text="{Binding SelectedArticle.CreatedAt, TargetNullValue='-'}" Foreground="{StaticResource TextPrimary}" FontWeight="Bold" />
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="3" Margin="0,0,0,16">
                    <TextBlock Text="Title" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}" FontSize="16" FontWeight="SemiBold"/>
                </StackPanel>

                <Grid Grid.Row="4" Margin="0,0,0,16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Body" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,6" />
                    <TextBox Grid.Row="1" Text="{Binding NewContent, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                             FontSize="14" />
                </Grid>

                <Grid Grid.Row="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <Button Style="{StaticResource PrimaryButton}" Content="Save Changes" Command="{Binding UpdateArticleCommand}" Padding="20,10"/>
                        <Button Style="{StaticResource DangerButton}" Content="Delete Article" Command="{Binding DeleteArticleCommand}" Padding="20,10"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <ProgressBar Width="120" Height="6" Minimum="0" Maximum="1" IsIndeterminate="{Binding IsLoading}" Margin="0,0,12,0"
                                     Foreground="{StaticResource BrandBlue}" Background="{StaticResource BorderLight}" BorderThickness="0"/>
                        <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondary}" FontStyle="Italic"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>